- module: apache
  access:
    enabled: true
    # Use the path as it will appear INSIDE the container
    var.paths: ["/var/log/apache2/access_log*"]
  error:
    enabled: true
    var.paths: ["/var/log/apache2/error_log*"]