filebeat.config.modules:
  path: ${path.config}/modules.d/*.yml
  reload.enabled: false

# Replace with your actual Elasticsearch/Logstash details
output.elasticsearch:
  hosts: ["elasticsearch:9200"]
  username: "elastic"
  password: "elastic"

setup.kibana:
  host: "kibana:5601"